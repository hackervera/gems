--- !ruby/object:RI::MethodDescription 
aliases: []

block_params: "block.arity == 1 ? request : [token, consumer_key, nonce, request.timestamp]"
comment: 
full_name: OAuth::Signature::Base::new
is_singleton: true
name: new
params: "(request, options = {}, &block) {|block.arity == 1 ? request : [token, consumer_key, nonce, request.timestamp]| ...}"
visibility: public
